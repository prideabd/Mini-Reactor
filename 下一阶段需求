
---

# ⚡ 阶段 8 项目工程大纲：AI Agent 智能运维与舆情管控并网

## 1. 核心需求背景（Why & Goal）

当前 C++ Mini-Reactor 引擎在阶段 7 完成了纯内存、无锁环形缓冲区（Ring Buffer）重构，在长连接高并发压测下能稳定跑出 1100+ QPS 的硬核性能。然而，高并发带来的硬件级长尾延迟（Tail Latency）以及开放式留言板的**恶意刷屏、垃圾广告和恶意攻击（DDoS/慢速连接）**，在没有人类运维干预时会使系统陷入裸奔。

本阶段的目标是**引入 AI Agent（智能体）作为系统的“高维大脑”**，由大语言模型（LLM）实现**自动化运维（AIOps）**、**留言区智能内容风控**以及**大盘整体运行健康度的自适应调整**。同时，维持 C++ 引擎的极致单周期吞吐性能，采取“核心异步解耦”的混合架构。

---

## 2. 系统总体物理架构（Architecture）

为防止大模型长达数百毫秒的推理时间（Latency）卡死 C++ Reactor 的单线程 `epoll` 核心，前后端与 Agent 之间必须做到**物理级异步隔离**：

1. **C++ 核心网关**：负责微秒级的纯内存无锁读写（大盘监控指标与 50 条环形缓冲区留言）。
2. **Python Agent 影子进程（后台运行）**：独立运行，定时通过本地/公网 HTTP 接口嗅探 C++ 引擎状态。它将原始数据加工为结构化 Prompt 投喂给大模型进行自主思考。
3. **前端 Web 大盘**：通过异步轮询接收 C++ 核心回喷的指标，并实时渲染 Agent 传回的诊断报告与控制台日志。

---

## 3. 具体功能需求与实现方案（Detailed Requirements）

### 需求一：C++ 后端加装“Agent 特权管理网关”（API层改造）（完成 + 测试完成）

为了让外部 Agent 进程看得到、管得着系统，必须在 `HttpHandler.cpp` 中新增两组专门面向智能体的结构化路由：

1. **`/api/agent/telemetry` (GET - 全测序上报接口)**：
* **要求**：高频无锁拼接。
* **业务行为**：将当前的 QPS 数值、CPU 滤波值、内存信息、当前的无锁环形队列中的最新 50 条留言合并为一个干净的 JSON。


2. **`/api/agent/control` (POST - 反向控制台接口)**：
* **要求**：由 C++ 原子变量或轻量策略列表进行拦截。
* **业务行为**：接受 Agent 的控制参数。例如：
* `block_nick`: 将某个恶意刷屏的昵称/IP 注入无锁黑名单；
* `cooldown_mode`: 指令开启后，临时对 `/api/comment` 进行前端按钮冷冻或后端秒级限流保护。





### 需求二：搭建 Python 影子 Agent 代理（大模型智能体层）（完成 + 测试完成）

利用 Python 编写一个低频（如每 3~5 秒）醒来一次的智能守护脚本。

1. **多智能体工具库（Tools）配置**：
* 赋予 Agent **“风控拦截工具”**：能够向 C++ 的 `/api/agent/control` 发送 POST，强制抹除某槽位留言内容或拉黑用户。
* 赋予 Agent **“自适应降级工具”**：当 QPS 过载导致宿主机压力高企时，能够向后端发送指令启动服务器的熔断或限流保护。


2. **AI 自主推理逻辑（Reasoning Loop）**：
* **舆情风控判定**：扫描 C++ 吐出的留言列表，如果发现包含“草”、“炸”、“垃圾”或者纯乱码小广告，大模型利用语义分析判定“违规”，自动触发工具将内存格子内容洗劫为 `[AI已拦截违规言论]`。
* **健康度自适应诊断**：大模型结合 QPS 和 CPU 滤波变化，如果判定网络管道正遭遇恶性挤压，自主决定开启熔断冷却模式。



### 需求三：前端霓虹控制大盘并网（UI 改造）

在 `index.html` 的四维图表右侧（或留言板下方）拉开一个全新的专属区域：`[⚡ AI Agent 智能运维自治终端 ⚡]`。

1. **AI 舆情周报/诊断日志框**：
* 渲染一个打字机特效的黑色命令行文本框。
* 实时展示 Agent 发出来的最新语义总结（例如：“当前留言板情绪：极客活跃度高，主要讨论底层原子锁问题；系统整体处于健康横盘状态。”）。


2. **智能防御动作闪烁警报**：
* 当 Agent 实施拉黑或系统降级控制时，前端网页面板会亮起**霓虹红/警告黄的警报灯特效**，并滚动打印：`[AI Action]: 拦截恶意刷屏用户 [Geek_xxx]，成功执行内存无锁擦除！`



---

## 4. 阶段 8 验收标准（Definition of Done）

1. **高并发性能不掉速**：当本地开启 `wrk` 以 1100+ QPS 疯狂轰炸阿里云服务器时，因为 Agent 处于异步独立进程，C++ 主 Reactor 引擎的**平均处理延迟依然维持在个位数毫秒或微秒级，吞吐量不产生断崖式失速**。
2. **无锁拦截绝对闭环**：在前端网页上使用脚本故意快速发送 10 条带有敏感言论的测试留言，3 秒内，网页右侧的 AI 终端正确打印诊断日志，且**留言板上的违规内容被大模型成功捕获并由 C++ 引擎执行纯内存无锁重写擦除**。
3. **断电回放完全相容**：即使 Agent 修改了留言板的内容，我们之前规划的“3秒低频后台异步落盘（AOF）”功能也能正确抓取被 AI 净化后的新数据写入磁盘，重启后加载出的依旧是健康的数据。

---


测试：
1.极端畸形报文注入测试（防崩溃鲁棒性测试）（完成）
2.系统一键级“紧急降级熔断”控制测试 （完成）
3.环形缓冲区“大数轮转覆盖”溢出测试 （完成） 网页前端问题：50个评论的控件貌似有问题，往上拉会导致回弹，同时拉到最上会直接变成最下，应该是设置为监控最下评论导致(解决)
4.高并发多线程“抢占与擦除”冲突测试（并发安全性测试）（完成） 高并发留言时，由于目前只保存50个，所以太多留言会导致，大模型推理结束准备擦除时，该留言已经消失了(目前不需要解决，这是很正常现象)
