
---
# 阶段9:
---
**留言系统升级需求文档**：

---

# 📝 留言系统升级需求文档 (PRD)

**版本:** v1.0

**主要模块:** 本地持久化、分布式黑名单、动态热加载

---

## 1. 背景与目标

### 1.1 背景

当前 `CommentRepository` 采用纯内存环形缓冲区（`MAX_MEM_COMMENTS = 50`）存储最新留言，具备极高的读写性能。然而，由于缺乏持久化机制，服务器重启会导致所有历史留言丢失。此外，随着业务发展，系统亟需对接分布式黑名单以拦截违规内容，并支持在不重启服务的情况下动态更新配置。

### 1.2 目标

* **零数据丢失（持久化）:** 保证内存中的环形缓冲区数据能够持久化到本地，并在服务器重启时完整恢复。
* **安全合规（分布式黑名单）:** 引入分布式黑名单机制，实时拦截/抹除违规用户的留言。
* **不停机维护（动态热加载）:** 支持黑名单规则或系统配置的动态热加载，确保高可用性。
* **性能守恒:** 升级后的系统不应显著降低原有的内存读写性能。

---

## 2. 核心功能需求

### 2.1 需求一：本地数据持久化 (Local Persistence)

* **功能描述:** 内存环形缓冲区的数据需要同步写入本地存储（如 SQLite、LevelDB 或轻量级 Append-Only Log 文件）。
* **业务规则:**
* **启动恢复:** 仓储层初始化时，应自动读取本地持久化文件，将最后 `MAX_MEM_COMMENTS` 条留言加载回内存环形缓冲区，并正确恢复 `g_comment_sequence` 的计数。
* **异步写入:** 为了不阻塞 `PushComment` 的主业务流程，数据的持久化操作应采用**异步线程**或定时批处理（Flush）机制。
* **擦除同步:** 当调用 `EraseComment` 抹除某条留言时，本地存储中的对应数据也必须同步更新或标记删除。



### 2.2 需求二：分布式黑名单 (Distributed Blacklist)

* **功能描述:** 在留言入库前或展示时，校验用户是否处于分布式黑名单中。
* **业务规则:**
* **多维度拦截:** 支持按 `nickname`（昵称）、`IP`（需扩展接口）或特定关键词进行拦截。
* **分布式同步:** 黑名单数据存储在分布式缓存（如 Redis）或通过配置中心统一分发。本地仓储需提供高效的查询结构（如内存 `std::unordered_set` 或布隆过滤器）进行本地快速匹配。
* **事后拦截:** 若用户在留言后才被加入黑名单，系统应能触发 `EraseComment` 对其历史留言进行原子抹除。

联动架构设计
```

[HTTP POST /api/comment] 
         │
         ▼
  httpDispatcher ──(1. 校验)──► BlacklistManager (高效内存读取，读写锁/原子指针)
         │                               │
         ├── [命中黑名单] ──► 返回 403     │ (2. 定时或通过 /api/admin/reload 触发)
         │                               ▼
         └── [通过校验] ──► CommentRepository::PushComment(...) ──► 异步写 SQLite

```



### 2.3 需求三：动态热加载 (Dynamic Hot-Reload)

* **功能描述:** 在不重启服务器的前提下，动态更新黑名单规则和系统控制参数。
* **业务规则:**
* **触发机制:** 支持通过外部信号（如 `SIGHUP`）、文件变动监听（如 `inotify`）或配置中心的回调通知触发加载。
* **无缝切换:** 热加载期间，`PushComment` 和 `GetLatestComments` 的读写请求不能被长时间阻塞。应采用 **双缓冲（Double Buffering）** 或原子指针切换（`std::atomic<std::shared_ptr<...>>`）来实现配置的无锁/低锁切换。



---

## 3. 非功能性需求与技术约束

### 3.1 性能与并发

* `PushComment` 的平均耗时增幅应控制在微秒级，绝不允许因为写磁盘或同步请求网络而导致接口卡顿。
* 保持现有的线程安全级别，避免因新引入的持久化/黑名单逻辑引发死锁（Deadlock）或竞态条件（Race Condition）。

### 3.2 容错与降级

* 若分布式黑名单服务（如 Redis）宕机，系统应能自动降级为“使用本地最后一次成功加载的黑名单快照”，并记录 Error 级别日志，保证核心留言功能不中断。

---

## 4. 关键接口变更预测 (Refactoring Preview)

为了实现上述需求，`CommentRepository` 的接口或内部结构预计会有以下调整：

```cpp
// 伪代码：预计需要扩充的内部组件或接口
class CommentRepository {
public:
    // 初始化，传入持久化路径及黑名单加载器
    static void Initialize(const std::string& db_path);
    
    // 原有接口保持兼容，但内部逻辑升级
    static void PushComment(const std::string& nickname, const std::string& content);
    
    // 触发热加载的接口
    static void ReloadBlacklist(); 
};

```

---


联动架构设计
```

[HTTP POST /api/comment] 
         │
         ▼
  httpDispatcher ──(1. 校验)──► BlacklistManager (高效内存读取，读写锁/原子指针)
         │                               │
         ├── [命中黑名单] ──► 返回 403     │ (2. 定时或通过 /api/admin/reload 触发)
         │                               ▼
         └── [通过校验] ──► CommentRepository::PushComment(...) ──► 异步写 SQLite

```