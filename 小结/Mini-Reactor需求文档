

---

# 📄 项目需求文档 (PRD)：Mini-Reactor 高性能网络并发引擎

## 1. 项目概述与目标

本项目旨在基于 Linux 原生系统调用，采用现代高性能网络服务器核心架构——**Master-Worker 主从 Reactor 模式**，构建一个高并发、低延迟的底层网络通信引擎。

项目完全从零（From Scratch）封装 Socket 编程接口、Epoll I/O 多路复用和 pthread 线程池。**核心设计理念是“底层 I/O 引擎与上层业务协议完全解耦”**。底层引擎通过严苛的非阻塞读写缓冲区管理，暴露出统一的事件回调接口，支撑上层自由切换 Redis 协议、IM 协议或 RPC 框架。

### 核心衡量指标

* **高并发**：支持万级以上并发连接（C10K 问题）。
* **高吞吐/低延迟**：利用边缘触发（ET）模式与非阻塞 I/O，最大化减少内核与用户态切换开销。
* **强扩展性**：提供清晰的 `onConnection` 和 `onMessage` 业务回调接口。

---

## 2. 核心系统架构

项目采用 **主从 Reactor (Master-Worker) 多线程模型**。

* **Main Reactor (主线程)**：仅负责监听 `Listen Socket`。当有新连接到来时，执行 `accept`，并按照**轮询 (Round-Robin)** 策略将新连接分发给具体的 Sub-Reactor。
* **Sub-Reactor Group (工作线程池)**：
* 线程池中每个线程运行一个独立的 `epoll_wait` 循环（One Loop Per Thread）。
* 负责管理分发到本线程的 `Client Socket` 的生命周期，进行数据的读、写以及业务逻辑处理。



---

## 3. 功能性需求 (Functional Requirements)

### 模块 A：网络与 I/O 核心 (Network & I/O Engine)

* **REQ-A1：非阻塞 Socket 封装**：支持将 Socket 设置为非阻塞模式 (`O_NONBLOCK`)。
* **REQ-A2：Epoll 边缘触发 (ET) 驱动**：必须使用 **`EPOLLET`** 模式管理读写事件。
* **REQ-A3：应用层环形/动态缓冲区 (Buffer)**：
* 实现循环 `recv` 直至返回 `EAGAIN`，数据暂存入输入缓冲区，交由状态机解析。
* 实现循环 `send` 直至数据发完或返回 `EAGAIN`（若未发完则动态注册 `EPOLLOUT` 监听）。



### 模块 B：并发与线程控制 (Concurrency & Threading)

* **REQ-B1：原生 pthread 线程池**：基于 `pthread_create`, `pthread_mutex_t`, `pthread_cond_t` 实现线程安全的基础线程池。
* **REQ-B2：跨线程异步唤醒 (Wakeup)**：使用 Linux 特有的 `eventfd` 实现低开销的跨线程事件通知，用于主线程唤醒正阻塞在 `epoll_wait` 的工作线程。

---

## 4. 上层业务协议支持（高度解耦的三个演进方向）

底层引擎完工后，通过实现回调接口，项目将演化为以下三个方向之一（或作为子模块并存）：

### 💡 方向一：高性能 Redis (RESP) 解析服务器（中间件方向）

* **核心功能**：手写状态机解析形如 `*3\r\n$3\r\nSET\r\n...` 的 RESP 字节流。在底层维护线程安全的 `std::unordered_map` 作为内存数据库，支持标准的 `GET`、`SET` 命令，无缝对接官方 `redis-cli`。

### 💡 方向二：自定义二进制私有协议的即时通讯 (IM) 服务器（应用服务方向）

* **核心功能**：定义特定的二进制报头（如：`2字节魔数 + 2字节命令ID + 4字节包体长度 + N字节Payload`），彻底杜绝网络粘包/拆包。维护全局用户路由表，引入时间轮算法，对超过 30 秒无心跳的僵尸连接执行强行断开。

### 💡 方向三：轻量级分布式 RPC (远程过程调用) 框架（分布式基础设施方向）

* **核心功能**：集成 `Google Protobuf` 将 C++ 对象转化为紧凑的二进制流。服务器端维护 `函数名 -> 函数指针` 的映射桩（Stub），利用 Mini-Reactor 的异步读写流实现请求的并发发送与结果的异步接收。

---

## 5. 迭代开发计划 (Milestones)

项目采取渐进式开发，拒绝“毕其功于一役”，共分为五个阶段：

### 📌 Milestone 1: 单线程非阻塞 Epoll Echo 服务器 (奠定基石)

* **目标**：跑通 Linux 底层 Socket 与 Epoll API 的基本闭环。
* **交付物**：包含 `main.cpp` 的单线程服务器。
* **核心任务**：
* [ ] 封装基础 Socket 创建、绑定、监听逻辑。
* [ ] 实现设置非阻塞 `fcntl(fd, F_SETFL, O_NONBLOCK)`。
* [ ] 实现单线程下的 `epoll_create`、`epoll_ctl`、`epoll_wait` 循环。
* [ ] 采用 **ET (边缘触发)** 模式，处理 `recv` 循环直至 `EAGAIN`。



### 📌 Milestone 2: 纯 C++ 线程池与事件通知机制 (并发骨架)

* **目标**：不涉及网络连接，纯粹攻克多线程并发与跨线程通信。
* **交付物**：可独立运行的 `ThreadPool` 模块测试用例。
* **核心任务**：
* [ ] 基于原生 `pthread` 封装通用的任务队列线程池。
* [ ] 使用 `pthread_mutex_t` 和 `pthread_cond_t` 保证任务分发的线程安全。
* [ ] 引入 Linux **`eventfd`** 系统调用，实现高效的跨线程异步唤醒机制（Wakeup）。

我们的下一个目标，就是利用 **C++ 面向对象（OOP）** 的思想，把这个简陋的单文件重构拆解为：

1. **`Channel` 类**：负责管理单个 `fd` 及其感兴趣的事件（读/写/错误）。
2. **`Epoll` 类**：封装红黑树的操作，只负责核心的调度与等待。
3. **`EventLoop` 类**：真正的反应堆（Reactor）核心，掌控主循环。


### 📌 Milestone 3: 主从 Reactor 融合与无锁分发 (架构合体)

* **目标**：将 Milestone 1 的 Epoll 驱动与 Milestone 2 的线程池完美融合成 Master-Worker 架构。
* **交付物**：多线程高并发 Reactor 网络引擎骨架。
* **核心任务**：
* [ ] 实现 `One Loop Per Thread`：为线程池中每个工作线程分配一个独立的 Epoll 实例（Sub-Reactor）。
* [ ] 主线程（Main Reactor）通过轮询（Round-Robin）算法接收新连接。
* [ ] 利用 `eventfd` 将新连接的 Socket 优雅地分发到目标工作线程的 Epoll 红黑树上。



### 📌 Milestone 4: 应用层双缓冲区设计 (彻底解决粘包/拆包)

* **目标**：解决 TCP 面向字节流带来的粘包、半包问题，使引擎具备产业级健壮性。
* **交付物**：具备高度抽象的 `Buffer` 类及稳定传输的 Echo 服务。
* **核心任务**：
* [ ] 为每个 TCP 连接配备独立的输入缓冲区（`Input Buffer`）和输出缓冲区（`Output Buffer`）。
* [ ] 完善 `EPOLLOUT` 事件的处理：当内核发送缓冲区满时（返回 `EAGAIN`），动态挂载 `EPOLLOUT`，待可写时继续发送，防止阻塞工作线程。



### 📌 Milestone 5: 业务层协议包装与极限压测 (打磨升华)

* **目标**：接入真正的业务协议（如选定的 Redis RESP），并进行极限压测。
* **交付物**：完整的成品项目 + 压测报告。
* **核心任务**：
* [ ] 解耦网络层，暴露 `onConnection` 与 `onMessage` 回调接口。
* [ ] 在 `onMessage` 中实现特定的协议解析器（如 Redis RESP 状态机）。
* [ ] 使用 `wrk` 或 `webbench` 进行高并发极限压测，利用 `valgrind` 进行内存泄漏和性能瓶颈分析。



---

## 6. 非功能性需求 (Non-Functional Requirements)

* **技术栈限定**：Linux (Ubuntu 20.04+), 现代 C++ (C++14/17), `<sys/epoll.h>`, `<pthread.h>`。
* **优雅关闭 (Graceful Shutdown)**：捕获 `SIGINT` (Ctrl+C)，安全释放所有资源，确保 **内存与文件描述符零泄漏**。
* **健壮性**：必须妥善处理 `EINTR`、`EPIPE` 等底层网络异常。

---
